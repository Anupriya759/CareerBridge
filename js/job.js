document.addEventListener('DOMContentLoaded',()=>{let id=Number(new URLSearchParams(location.search).get('id')),j=JOBS.find(x=>x.id===id),el=qs('#jobDetail');if(!j){el.innerHTML='<div class="info-card"><h2>Job not found</h2><a class="btn btn-primary" href="jobs.html">Back to jobs</a></div>';return}el.innerHTML=`<article class="detail-card"><div class="detail-head"><div><span class="tag">${j.type}</span><h1>${j.title}</h1><p><strong>${j.company}</strong> · ${j.location}</p></div><button class="btn ${isSaved(j.id)?'btn-outline':'btn-primary'}" onclick="toggleSave(${j.id});this.textContent=isSaved(${j.id})?'★ Saved':'☆ Save'">${isSaved(j.id)?'★ Saved':'☆ Save'}</button></div><div class="job-meta">${j.skills.map(s=>`<span class="tag">${s}</span>`).join('')}<span class="tag">${j.mode}</span><span class="tag">${j.experience}</span></div><div class="detail-section"><h3>Salary</h3><p class="salary">₹${j.salary} LPA</p></div><div class="detail-section"><h3>Job description</h3><p>${j.desc}</p></div><div class="detail-section"><h3>How to apply</h3><p>Register or login to CareerBridge and submit your application to the recruiter.</p><a class="btn btn-primary" href="login.html">Login to Apply</a></div></article>`})
