document.addEventListener('DOMContentLoaded',()=>{let name=new URLSearchParams(location.search).get('name'),c=COMPANIES.find(x=>x.name===name),el=qs('#companyDetail');if(!c){el.innerHTML='<div class="info-card"><h2>Company not found</h2></div>';return}let jobs=JOBS.filter(j=>j.company===c.name);el.innerHTML=`<div class="detail-card"><div class="company-logo">${c.icon}</div><h1>${c.name}</h1><p>📍 ${c.city}</p><p>${c.about}</p><div class="detail-section"><h3>Open positions (${jobs.length})</h3><div class="job-list">${jobs.map(jobCard).join('')}</div></div></div>`})
