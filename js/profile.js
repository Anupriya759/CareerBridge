document.addEventListener('DOMContentLoaded',()=>{let u=getCurrentUser();if(!u){location.href='login.html';return}qs('#profileName').value=u.name||'';qs('#profileEmail').value=u.email||'';qs('#profileSkills').value=u.skills||'';qs('#profileAbout').value=u.about||'';qs('#profileForm').addEventListener('submit',e=>{e.preventDefault();u.name=qs('#profileName').value;u.email=qs('#profileEmail').value;u.skills=qs('#profileSkills').value;u.about=qs('#profileAbout').value;localStorage.setItem('careerbridge_user',JSON.stringify(u));let m=qs('#profileMessage');m.textContent='Profile saved successfully.';m.className='message show'})})
