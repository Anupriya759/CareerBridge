document.addEventListener('DOMContentLoaded',()=>{
let rf=qs('#registerForm');if(rf)rf.addEventListener('submit',e=>{e.preventDefault();let user={name:qs('#regName').value,email:qs('#regEmail').value,password:qs('#regPassword').value,role:qs('#regRole').value};localStorage.setItem('careerbridge_user',JSON.stringify(user));let m=qs('#registerMessage');m.textContent='Account created successfully! Redirecting...';m.className='message show';setTimeout(()=>location.href='profile.html',700)});
let lf=qs('#loginForm');if(lf)lf.addEventListener('submit',e=>{e.preventDefault();let email=qs('#loginEmail').value,password=qs('#loginPassword').value,user=JSON.parse(localStorage.getItem('careerbridge_user')||'null');let m=qs('#loginMessage');if(user&&user.email===email&&user.password===password){m.textContent='Login successful!';m.className='message show';setTimeout(()=>location.href='profile.html',500)}else{m.textContent='Demo mode: create an account first, then login with the same details.';m.className='message show'}})
})
